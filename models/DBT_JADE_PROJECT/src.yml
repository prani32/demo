version: 2

sources:
  - name: shipping_data
    description: A shipping data database.
    database: DBT_DB
    schema: DBT_SCHEMA
    tables:
      - name: PO_ACK_855
        description: Raw Purchase order.
      - name: ADJ_ADV_947
        description: Primary key for customers.
        loaded_at_field: _etl_loaded_at
        freshness:
          warn_after: {count: 12, period: hour}
          error_after: {count: 24, period: hour}